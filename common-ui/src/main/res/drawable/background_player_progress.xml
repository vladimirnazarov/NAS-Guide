<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!--  Background  -->
    <item android:id="@android:id/background">

        <shape android:shape="rectangle">

            <corners android:radius="@dimen/exhibit_player_corners"/>
            <solid android:color="@color/exhibit_player_background"/>
            <size android:height="@dimen/exhibit_player_height"/>

        </shape>

    </item>

    <!--  Secondary Progress  -->
    <item android:id="@android:id/secondaryProgress">

        <clip>

            <shape android:shape="rectangle">

                <corners android:radius="@dimen/exhibit_player_corners"/>
                <solid android:color="@color/exhibit_player_foreground"/>
                <size android:height="@dimen/exhibit_player_height"/>

            </shape>

        </clip>

    </item>

    <!--  Secondary Progress  -->
    <item android:id="@android:id/progress">

        <clip>

            <shape android:shape="rectangle">

                <corners android:radius="@dimen/exhibit_player_corners"/>
                <solid android:color="@color/exhibit_player_foreground"/>
                <size android:height="@dimen/exhibit_player_height"/>

            </shape>

        </clip>

    </item>

</layer-list>